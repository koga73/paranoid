module.exports = {
	NAME:"Dispatch",
	VERSION:"0.0.1",
	
	SERVER_PORT:8888, //Used for both WebServer and SocketServer
	PING_INTERVAL:10000 //Number of milliseconds to ping clients to ensure active connections
};